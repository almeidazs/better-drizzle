Use `AGENTS.md` as the repository-wide source of truth.

If Claude-compatible agent skills are available, also load `skills/better-drizzle/SKILL.md` and only read the `references/` files that match the task you are doing.
