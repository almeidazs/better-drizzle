/**
 * Base shape for a Better Drizzle plugin. Plugins are plain objects passed via
 * {@link BetterClientOptions.plugins} and can extend the client with additional
 * behaviour or capabilities.
 */
export type Plugin = object;
